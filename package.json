{
  "name": "cautious-system",
  "version": "1.0.0",
  "license": "MIT",
  "repository": {
    "type" : "git",
    "url": "git+https://github.com/RA80533/cautious-system.git"
  },
  "scripts": {
    "build": "bash ./etc/build.sh || true",
    "clean": "bash ./etc/clean.sh || true",
    "lint": "true",
    "patch": "bash ./etc/patch.sh || true",
    "start": "bash ./etc/start.sh || true",
    "test": "true"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "private": true
}
